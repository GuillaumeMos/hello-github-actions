name: ex3
on: push

jobs:
  build:
      name: test
      runs-on: ubuntu-latest
      steps:
         - run: sudo apt-get install -y bats
         - uses: ./action-a
         - run: bats ./action-a/test.sh
        
